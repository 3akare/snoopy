version: "3.8"

services:
  client:
    build: ./client
    ports:
      - "80:80"
  server:
    build: ./server
    ports:
      - "3000:3000"
    environment:
      - FLASK_ENV=production
      - FLASK_DEBUG=False
volumes:
  snoopy:
